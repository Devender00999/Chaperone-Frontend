const config = {
   apiUrl: process.env["REACT_APP_CHAPERONE_API"],
   url: process.env["REACT_APP_CHAPERONE_URL"],
};

console.log(process.env);
export const colors = {
   primary: "#ff6600",
   primaryLight: "#ff751a",
};

export default config;
